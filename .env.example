# Copy this file to .env.local and fill in a real endpoint once a backend
# or email service (e.g. a serverless function, Formspree, EmailJS) exists.
# Never commit real secrets — this variable only holds a public endpoint URL.
VITE_CONTACT_ENDPOINT=
